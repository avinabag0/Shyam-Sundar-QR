<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Shyam Sundar | Connect With Us</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: "Poppins", sans-serif;
      background: #a40014;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      overflow: hidden;
      color: white;
    }

    /* Background animation */
    .bg-gradient {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 30% 50%, rgba(255, 200, 200, 0.2), transparent 60%),
                  radial-gradient(circle at 70% 80%, rgba(255, 255, 255, 0.15), transparent 60%),
                  radial-gradient(circle at 50% 20%, rgba(255, 180, 180, 0.2), transparent 60%);
      animation: gradientShift 10s ease-in-out infinite;
      z-index: 0;
      transition: transform 0.2s ease;
    }

    @keyframes gradientShift {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.7; }
    }

    .container {
      max-width: 600px;
      width: 100%;
      position: relative;
      z-index: 2;
      text-align: center;
      animation: fadeInUp 0.8s ease-out;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Logo */
    .logo-container {
      margin-bottom: 30px;
      display: inline-block;
      position: relative;
    }

    .brand-logo {
      width: 110px;
      height: 110px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid rgba(255, 255, 255, 0.8);
      box-shadow: 0 0 30px rgba(255, 255, 255, 0.3);
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    h1 {
      font-size: 38px;
      font-weight: 700;
      margin-bottom: 10px;
      letter-spacing: 1px;
      background: linear-gradient(135deg, #fff 0%, #f5e4e4 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .subtitle {
      color: rgba(255, 255, 255, 0.8);
      font-size: 17px;
      margin-bottom: 40px;
    }

    /* Social grid */
    .social-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 25px;
      margin-bottom: 50px;
    }

    .social-item {
      position: relative;
      aspect-ratio: 1;
      cursor: pointer;
      border-radius: 25px;
      overflow: hidden;
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .social-link {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      width: 100%;
      height: 100%;
      text-decoration: none;
      color: white;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 25px;
      transition: all 0.3s ease;
    }

    .social-item:hover {
      transform: translateY(-8px) scale(1.05);
    }

    .social-item:hover .social-link {
      background: rgba(255, 255, 255, 0.1);
      border-color: rgba(255, 255, 255, 0.25);
    }

    .social-icon {
      font-size: 42px;
      margin-bottom: 10px;
      transition: transform 0.3s ease;
    }

    .social-item:hover .social-icon {
      transform: scale(1.1);
    }

    .social-name {
      font-size: 14px;
      font-weight: 600;
      opacity: 0.9;
      letter-spacing: 0.5px;
    }

    /* Brand-specific colors */
    .facebook .social-icon { color: #1877F2; text-shadow: 0 0 12px rgba(24, 119, 242, 0.6); }
    .instagram .social-icon { color: #E4405F; text-shadow: 0 0 12px rgba(228, 64, 95, 0.6); }
    .youtube .social-icon { color: #FF0000; text-shadow: 0 0 12px rgba(255, 0, 0, 0.6); }

    /* CTA Button */
    .open-all-btn {
      position: relative;
      padding: 18px 45px;
      background: linear-gradient(135deg, #8b0000, #ff4d4d);
      color: white;
      border: none;
      border-radius: 50px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 10px 40px rgba(255, 100, 100, 0.3);
      overflow: hidden;
    }

    .open-all-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: left 0.5s ease;
    }

    .open-all-btn:hover::before {
      left: 100%;
    }

    .open-all-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 50px rgba(255, 100, 100, 0.5);
    }

    .open-all-btn:active {
      transform: translateY(-1px);
    }

    .footer {
      text-align: center;
      margin-top: 40px;
      color: rgba(255, 255, 255, 0.4);
      font-size: 14px;
      font-weight: 300;
    }

    /* Floating thread particles */
    .particles {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 1;
    }

    .particle {
      position: absolute;
      width: 4px;
      height: 4px;
      background: rgba(255, 255, 255, 0.4);
      border-radius: 50%;
      animation: particleFloat 15s infinite;
    }

    @keyframes particleFloat {
      0% { transform: translateY(100vh) translateX(0); opacity: 0; }
      10% { opacity: 1; }
      90% { opacity: 1; }
      100% { transform: translateY(-100vh) translateX(100px); opacity: 0; }
    }

    @media (max-width: 600px) {
      h1 { font-size: 30px; }
      .social-grid { grid-template-columns: repeat(2, 1fr); gap: 18px; }
      .social-icon { font-size: 34px; }
      .open-all-btn { padding: 15px 35px; font-size: 16px; }
    }
  </style>
</head>
<body>
  <div class="bg-gradient"></div>
  <div class="particles" id="particles"></div>

  <div class="container">
    <div class="logo-container">
      <img src="Shyam Sundar.jpg" alt="Shyam Sundar Logo" class="brand-logo">
    </div>
    <h1>Connect With Us</h1>
    <p class="subtitle">Stay connected with Shyam Sundar across platforms</p>

    <div class="social-grid">
      <div class="social-item facebook">
        <a href="https://www.facebook.com/syamsundarsalkia" target="_blank" class="social-link">
          <div class="social-icon">📘</div>
          <div class="social-name">Facebook</div>
        </a>
      </div>

      <div class="social-item instagram">
        <a href="https://www.instagram.com/shyamsundarsalkia" target="_blank" class="social-link">
          <div class="social-icon">📸</div>
          <div class="social-name">Instagram</div>
        </a>
      </div>

      <div class="social-item youtube">
        <a href="https://www.youtube.com/@shyamsundarsalkiahowrah5493" target="_blank" class="social-link">
          <div class="social-icon">▶️</div>
          <div class="social-name">YouTube</div>
        </a>
      </div>
    </div>

    <button class="open-all-btn" onclick="openAllLinks()">✨ Open All Platforms</button>
    <div class="footer">Scan · Connect · Engage</div>
  </div>

  <script>
    // Floating thread-like particles
    function createParticles() {
      const particlesContainer = document.getElementById('particles');
      const particleCount = 35;
      for (let i = 0; i < particleCount; i++) {
        const particle = document.createElement('div');
        particle.className = 'particle';
        particle.style.left = Math.random() * 100 + '%';
        particle.style.animationDelay = Math.random() * 15 + 's';
        particle.style.animationDuration = (15 + Math.random() * 10) + 's';
        particlesContainer.appendChild(particle);
      }
    }
    createParticles();

    // Open all links at once
    function openAllLinks() {
      const links = [
        'https://www.facebook.com/syamsundarsalkia',
        'https://www.instagram.com/shyamsundarsalkia',
        'https://www.youtube.com/@shyamsundarsalkiahowrah5493'
      ];
      links.forEach((link, index) => {
        setTimeout(() => window.open(link, '_blank'), index * 200);
      });
    }

    // Animate social grid on load
    document.addEventListener('DOMContentLoaded', () => {
      const items = document.querySelectorAll('.social-item');
      items.forEach((item, index) => {
        item.style.animation = `fadeInUp 0.6s ease-out ${index * 0.1}s both`;
      });
    });

    // Mouse movement gradient shift
    document.addEventListener('mousemove', e => {
      const x = e.clientX / window.innerWidth;
      const y = e.clientY / window.innerHeight;
      document.querySelector('.bg-gradient').style.transform = `translate(${x * 20}px, ${y * 20}px)`;
    });
  </script>
</body>
</html>
